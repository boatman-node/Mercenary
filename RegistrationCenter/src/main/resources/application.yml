server:
  port: 8761

eureka:
  instance:
    hostname: RegistrationCenter
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8762/eureka/
spring:
  application:
    name: RegistrationCenter